# DevSpec Environment Configuration Template
# Copy this file to .env and customize as needed

# === Debug Mode ===
# Enable debug mode for CLI command logging
# Valid values: true, false, 1, 0, yes, no
DEBUG=false

# === Database Configuration ===
# Override default database path
# DEVSPEC_DATABASE_PATH=.specgraph/.runtime/specgraph.db

# === Logging Configuration ===
# Override default logging level
# Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEVSPEC_LOGGING_LEVEL=INFO

# Override default logging format
# Valid values: rich, simple
# DEVSPEC_LOGGING_FORMAT=rich

# === Spec Configuration ===
# Override default spec root directory
# DEVSPEC_SPEC_ROOT=.specgraph

# === Note ===
# Environment variables follow this naming convention:
# - Use DEVSPEC_ prefix for all config overrides
# - Use uppercase with underscores (e.g., DEVSPEC_DATABASE_PATH)
# - Variables are converted to lowercase dot-notation (e.g., database.path)
#
# Priority order (highest to lowest):
# 1. CLI parameters (e.g., --debug)
# 2. System environment variables (e.g., export DEVSPEC_DEBUG=true)
# 3. .env file (this file)
# 4. YAML config file (.specgraph/config.yaml)
# 5. Default values (in code)
